<h1>Listing: <%= @listing.title %></h1>

<p><%= link_to "Edit listing", edit_listing_path(@listing) if can? :update, @listing %></p>

<%= image_tag @listing.image if @listing.image.attached? %>

<p>
  <strong>Posted:</strong> <%= @listing.created_at %> by <%= link_to @listing.user.username, user_profile_path(@listing.user) %>
</p>

<p>
  <strong>Platform:</strong> <%= @listing.platform.manufacturer %> <%= @listing.platform.name %>
</p>

<p>
  <strong>Genre:</strong> <%= @listing.genre.name %>
</p>

<p>
  <strong>Condition:</strong> <%= @listing.condition.humanize %>
</p>

<h1><%= number_to_currency(@listing.price) %></h1>

<p>
  <strong>Description:</strong><br>
  <%= @listing.description %>
</p>